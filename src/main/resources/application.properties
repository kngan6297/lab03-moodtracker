# Main application configuration
# Use YAML profiles for environment-specific settings

# Default profile (if none specified)
spring.profiles.active=dev

# Common settings
spring.application.name=mood-tracker 